info:
  name: fakejs - random user data
  type: http
  seq: 5

http:
  method: POST
  url: https://echo.usebruno.com
  body:
    type: json
    data: |-
      {
        "Title": "{{$randomNamePrefix}}",
        "Name": "{{$randomFullName}}",
        "Role": "{{$randomInt}}"
      }
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

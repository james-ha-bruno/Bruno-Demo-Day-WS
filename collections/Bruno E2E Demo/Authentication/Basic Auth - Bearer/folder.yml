info:
  name: Basic Auth / Bearer
  type: folder
  seq: 1

request:
  auth:
    type: oauth2
    flow: authorization_code
    credentials:
      placement: body
    tokenConfig:
      id: credentials
      placement:
        header: Bearer
      source: access_token
